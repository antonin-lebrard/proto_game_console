name: proto_game_console
version: 0.0.1
description: proto_game in console mode (stdin/stdout)


dependencies:
  proto_game:
    path: "../proto_game"
  dart_console:
    path: "../dart-console"

environment:
  sdk: '>=1.0.0 <2.0.0'

dev_dependencies:
  test: '>=0.12.0 <0.13.0'